#!/bin/bash
set -euo pipefail

REPO_ROOT=$(git rev-parse --show-toplevel)
cd "$REPO_ROOT"

echo "Running full validation (pnpm install --frozen-lockfile && pnpm build)..."
pnpm install --frozen-lockfile
pnpm build
